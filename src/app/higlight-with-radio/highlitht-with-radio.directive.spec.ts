import { HighlithtWithRadioDirective } from './highlitht-with-radio.directive';

describe('HighlithtWithRadioDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlithtWithRadioDirective();
    expect(directive).toBeTruthy();
  });
});
