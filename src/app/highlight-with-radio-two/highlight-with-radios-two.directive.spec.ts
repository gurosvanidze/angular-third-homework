import { HighlightWithRadiosTwoDirective } from './highlight-with-radios-two.directive';

describe('HighlightWithRadiosTwoDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightWithRadiosTwoDirective();
    expect(directive).toBeTruthy();
  });
});
